---
import { withBase } from '../lib/url';
import { siteConfig } from '../site.config';
---

<footer class="site-footer" data-pagefind-ignore>
  <div class="footer-inner">
    <p>
      <strong>{siteConfig.shortName} · {siteConfig.name}</strong> — Markdown trong
      Git là nguồn dữ liệu gốc.
    </p>
    <p><a href={withBase('search/')}>Tìm trong toàn bộ kho tri thức</a></p>
  </div>
</footer>
